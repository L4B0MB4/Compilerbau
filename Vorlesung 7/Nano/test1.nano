Integer Euklid(Integer a, Integer b)
{
   while(a != b)
   {
      if(a > b)
      {
         a = a---b;
      }
      else
      {
         b = b-a;
      }
   }
   return a;
}


Integer main(String arg1, String arg2)
{
   print StrCat(IntToStr(Euklid(StrToInt(arg1), StrToInt(arg2))), "\n");
   return 0;
}